
"""
It's brige that connects the Eventloop Flagging task as done to fd(file_descriptor) calling the _wakeup() after receving i/o resutls.
I/O Multiplexer is what set_result() in the eventloop . 
"""
